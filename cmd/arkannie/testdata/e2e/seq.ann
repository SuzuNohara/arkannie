# ann v0.1
// E2E-seq: two dispatches chained by a binding. The second dispatch receives
// the payload the first produced, so its echo carries the first result too.
// The chained result is emitted explicitly via [return].
$r = [echo] : hola
$chained = [echo] : $r
[return] --id=chained $chained
